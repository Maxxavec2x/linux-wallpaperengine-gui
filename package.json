{
	"name": "linux-wallpaperengine-gui",
	"version": "0.5.2",
	"type": "module",
	"description": "A graphical user interface for managing wallpapers on Linux, powered by Go(Backend) Electron(Frontend) and linux-wallpaperengine.",
	"author": {
		"name": "AzPepoze",
		"email": "azpepoze@gmail.com"
	},
	"main": "build/frontend/electron/main.js",
	"scripts": {
		"dev": "bun run build:backend && ./build/backend/linux-wallpaperengine-gui",
		"dev:frontend": "vite",
		"start": "bun run build && ./dist/linux-unpacked/linux-wallpaperengine-gui --debug-mode",
		"build": "bun run build:backend && vite build && electron-builder --linux --dir",
		"build:all": "bun run build:backend && vite build && electron-builder --linux AppImage deb rpm --publish never",
		"build:backend": "mkdir -p build/backend && cd src/backend && go build -o ../../build/backend/linux-wallpaperengine-gui main.go",
		"preview": "vite preview",
		"check": "bun utils/check.ts",
		"check:frontend": "bun utils/check.ts --frontend",
		"check:backend": "bun utils/check.ts --backend",
		"i18n:check": "bun utils/i18n-check.ts",
		"i18n:generate": "bun utils/i18n-generate.ts"
	},
	"trustedDependencies": [
		"electron"
	],
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^7.0.0",
		"@tsconfig/svelte": "^5.0.8",
		"@types/colorthief": "^2.6.1",
		"@types/markdown-it": "^14.1.2",
		"@types/node": "^25.6.0",
		"electron": "^41.2.2",
		"electron-builder": "^26.8.1",
		"markdown-it": "^14.1.1",
		"sass": "^1.99.0",
		"svelte": "^5.55.4",
		"svelte-check": "^4.4.6",
		"typescript": "~6.0.3",
		"vite": "^7.3.2",
		"vite-plugin-electron": "^0.29.1",
		"vite-plugin-electron-renderer": "^0.14.6"
	},
	"dependencies": {
		"colorthief": "^3.3.1",
		"material-icons": "^1.13.14",
		"steamworks.js": "^0.4.0"
	},
	"build": {
		"icon": "src/public/icon.png",
		"directories": {
			"output": "dist"
		},
		"extraResources": [
			{
				"from": "build/backend/linux-wallpaperengine-gui",
				"to": "linux-wallpaperengine-gui"
			},
			{
				"from": "build/frontend/icon.png",
				"to": "icon.png"
			}
		],
		"files": [
			"build/frontend/**/*"
		],
		"linux": {
			"artifactName": "${productName}.${ext}",
			"executableName": "linux-wallpaperengine-gui",
			"category": "Utility"
		}
	}
}
